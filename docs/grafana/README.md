# Статистика Agent Web в Grafana Cloud

Agent Web отправляет небольшие агрегаты в Grafana Cloud по HTTPS раз в минуту.
Установка Alloy, Prometheus или открытие портов на компьютерах друзей не нужны.
Наличие непустого `telemetry.token` включает отправку; отдельного переключателя нет.
Без токена нет сетевых обращений телеметрии и не создаётся её файл состояния.

## 1. Получите параметры подключения

1. Войдите в [Grafana Cloud Portal](https://grafana.com/auth/sign-in/).
2. Выберите свой stack, откройте его Grafana (Launch).
3. В разделе Connections / Add new connection найдите **OpenTelemetry**.
   Названия пунктов могут немного меняться. Нужна страница настройки отправки
   OTLP, а не адрес страницы дашборда и не Prometheus remote_write URL.
4. Скопируйте **OTLP endpoint** и **Instance ID** для OTLP. Например,
   `https://otlp-gateway-prod-REGION.grafana.net/otlp`. Используйте именно
   значения своего стека; номер из адреса Grafana не обязательно равен OTLP Instance ID.
5. Создайте в Cloud Portal **Access Policy**, ограниченную этим stack,
   с единственным scope **metrics:write**. В зависимости от интерфейса этот
   раздел находится в Security / Access Policies либо в Administration / Users
   and access / Cloud access policies. Создайте token для этой policy и сохраните
   показанное значение. Это Cloud access policy token, не Grafana service account token.
6. Для каждого друга можно создать отдельный token той же policy: тогда его
   получится отозвать отдельно. Запишите срок действия, если его задаёте.

Обычный токен с правом записи не позволяет читать метрики и управлять дашбордами,
но его владелец может прислать произвольные метрики и расходовать вашу квоту.
Не коммитьте токены в Git. Отправляйте друзьям конфигурацию лично.

## 2. Добавьте один блок в config.json

При запуске штатными скриптами файл находится в `data/config.json` в каталоге
проекта. В этой установке это `F:\dev\remote\data\config.json`.
При запуске с другим `--data-dir` редактируйте `config.json` именно в том каталоге.
Без `--data-dir` путь показывает `uv run agent-web doctor` (строка Data).

Добавляемая строка (подставьте три значения из предыдущего шага):

```json
"telemetry": {"endpoint": "https://otlp-gateway-prod-REGION.grafana.net/otlp", "instance_id": "YOUR_OTLP_INSTANCE_ID", "token": "YOUR_METRICS_WRITE_TOKEN"}
```

Пример целого файла:

```json
{
  "roots": ["F:\\dev"],
  "telemetry": {
    "endpoint": "https://otlp-gateway-prod-REGION.grafana.net/otlp",
    "instance_id": "YOUR_OTLP_INSTANCE_ID",
    "token": "YOUR_METRICS_WRITE_TOKEN"
  }
}
```

Сохраните свои `roots` и все другие существующие поля. Перед новым полем нужна
запятая; после последнего поля запятой быть не должно. Передавайте исходный token,
без `Basic`, без base64-кодирования и без заголовка Authorization.
Приложение само добавит `/v1/metrics` к базовому OTLP endpoint.
Полный адрес, уже заканчивающийся на `/v1/metrics`, тоже поддерживается.

## 3. Перезапустите приложение

Дождитесь завершения текущих ответов и из корня проекта выполните:

```powershell
.\scripts\restart-agent-web.ps1
```

На macOS: `bash scripts/restart-agent-web.sh`.
Если сервер работает в терминале, остановите его Ctrl+C и запустите снова.
Изменение/удаление токена вступает в силу после перезапуска. Чтобы отключить
отправку, удалите `token` или весь блок `telemetry`, затем перезапустите сервер.

При первом включении фиксируется исходная граница: ранее накопленная история
не включается в счётчики. Создайте новый чат и отправьте сообщение, затем
подождите 2–3 минуты. Первый пакет уходит сразу, следующие — примерно раз в минуту.

## 4. Проверьте поступление данных

1. В Grafana откройте **Explore**.
2. Выберите источник **Prometheus / Grafana Cloud Metrics** своего стека
   (имя часто выглядит как `grafanacloud-...-prom`).
3. Переключите редактор на Code, введите `agent_web_online` и нажмите Run query.
4. Значение `1` означает, что пакет метрик поступил. Затем проверьте
   `agent_web_messages_sent_total` и `agent_web_chats_created_total`.

Если данных нет, проверьте endpoint, OTLP Instance ID, scope и срок токена.
Локальные предупреждения находятся в `data/logs/agent-web.err.log` при запуске
скриптом перезапуска. HTTP 401/403 обычно означает проблему авторизации,
404 — неверный адрес, 429 — ограничение приёма. Тело ответа, token и подробности
сетевого исключения специально не пишутся в журнал телеметрии.
После ошибки интервал повторов растёт до 15 минут; перезапуск запускает попытку сразу.
`Telemetry disabled` означает ошибку настроек или файла состояния.

## 5. Импортируйте готовый дашборд

1. Откройте **Dashboards → New → Import** (либо кнопку Import).
2. Нажмите **Upload dashboard JSON file** и выберите
   [agent-web-dashboard.json](agent-web-dashboard.json) из этой папки.
   Можно открыть файл в редакторе и вставить его содержимое в поле JSON.
3. В поле **Grafana Cloud Metrics** выберите тот же источник Prometheus,
   который проверили в Explore.
4. Выберите папку, нажмите **Import**.
5. Оставьте период Last 24 hours, автообновление 1m. При повторном импорте
   существующий дашборд с UID `agent-web-usage` можно перезаписать.

Дашборд содержит активные установки, серверы на связи, новые чаты и сообщения,
успешные ответы, сбои/прерывания, долю сбоев, время ответа и разбивки по
агентам, категориям ошибок и версиям. До появления ответов p95/медиана могут
показывать No data. Нулевые счётчики без использования нормальны.

## Что означают данные и что отправляется

- **Установка** — случайный UUID в `data/telemetry-state.json`, а не человек.
  Не копируйте этот файл или рабочую БД друзьям: у каждой установки должны быть свои данные.
- **Активность** — создание чата или приём сообщения через Agent Web.
  Открытая страница/работающий сервер сами по себе активностью не считаются.
- **Сообщение** — принятый пользовательский запрос, один на `client_request_id`.
  Импорт истории, синхронизация внешних сообщений, кусочки потокового ответа
  и внутренние повторы агента не увеличивают этот счётчик.
- **Ошибки** — исключения при выполнении запроса агента. Это не сбор всех
  исключений браузера, HTTP API или операционной системы. Категории ограничены
  `timeout`, `connection`, `authentication`, `internal`, `unknown` и выводятся
  из разрешённого списка классов исключений. `unknown` не раскрывает исходный класс.
- **Прерывания** — запросы, помеченные неудачными после перезапуска сервера.
  Они учитываются в доле неудачных запросов отдельно от категорий исключений.
- **Время ответа** — от приёма до завершения/ошибки, включая повторы и ожидание;
  исходные отметки имеют секундную точность. Прерванные перезапуском запросы
  исключаются. Квантили приблизительные, рассчитаны по фиксированной гистограмме.
- В пакете только агрегированные числа, время, случайный UUID, версия Python-пакета
  Agent Web, агент (`codex`, `opencode`, `other`), результат и категория ошибки.
  Тексты, пути, названия проектов/чатов, native ID, имя компьютера, модель,
  содержимое исключений, файлы и логи не отправляются.
- Это **псевдонимная** статистика: UUID позволяет сопоставлять посещения одной
  установки. Принимающая сторона технически видит IP HTTPS-соединения; IP не
  добавляется в метрики. Токен тоже может связывать отправителя с выданным доступом.
- Счётчики накопительные и вычисляются из сохранённых audit events. Повторная
  отправка не прибавляет прежний пакет второй раз. При недоступности сети следующий
  пакет отражает накопленный итог; точное распределение событий внутри пропуска
  не восстанавливается. `increase()` в Grafana — оценка, возможны дробные числа и
  недоучёт активности до первого успешного пакета. Для биллинга данные не подходят.
- Отключение токена останавливает отправку. Локальный аудит приложения продолжает
  работать, поэтому после повторного включения с прежним файлом состояния
  накопленные за паузу события тоже попадут в итог. Если нужна новая граница,
  остановите сервер и переименуйте `telemetry-state.json`: будет создан новый UUID.
  После восстановления старой БД также начинайте новый файл состояния.
- Ошибки отправки не влияют на ответы. Одновременно выполняется один запрос,
  сетевые таймауты — 10 секунд, перенаправления отключены. Используется прямое
  HTTPS-соединение без proxy-переменных окружения. Сканирование агрегатов выполняется
  в фоне; отдельной очереди с содержимым сообщений нет. Новых миграций БД не требуется.

Число временных рядов ограничено наборами меток (примерно несколько десятков
на установку), но общая квота зависит от количества установок и смены версий.
Следите за Usage в Grafana Cloud; бесплатность в пределах любого масштаба не гарантируется.

Официальные справки: [OTLP endpoint](https://grafana.com/docs/grafana-cloud/send-data/otlp/send-data-otlp/),
[Access Policies](https://grafana.com/docs/grafana-cloud/security-and-account-management/authentication-and-permissions/access-policies/),
[импорт дашбордов](https://grafana.com/docs/grafana/latest/dashboards/build-dashboards/import-dashboards/).
